# HyperBid Flutter SDK demo — iOS Podfile.
#
# Standard Flutter Podfile scaffolding plus the HyperBid core SDK and the iOS
# Ad Network adapter pods (including the AdMob and Max cross-mediation
# adapters), covering the same Ad Networks as `android/app/mcsdk.gradle`.
#
# iOS pod versions are independent of the Android Gradle versions — regenerate
# the pod list from the HyperBid SDK Download Center
# (https://portal.hyperbid.com/m/sdk/download?type=iOS) to change versions or
# the set of integrated Ad Networks.
#
# After editing this file run:  cd ios && pod install --repo-update
# Reference: SDK-Import-and-Initialization.md §2

# iOS minimum deployment target is 13.0.
platform :ios, '17.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  target 'RunnerTests' do
    inherit! :search_paths
  end

  pod "HyperbidToolsMCSDK", "3.9.10"
  pod "MCSDKMaterialPlugin", "1.1.0"
  pod "HyperbidToolsMCToponAdapter", "6.5.73.0"
  pod "TPNMediationAdxSmartdigimktAdapter", "6.5.75.2.0"
  pod "HyperbidToolsMCMaxAdapter", "13.6.0.2"
  pod "HyperbidToolsMCAdmobAdapter", "13.4.0.0"

  pod "TPNMediationMolocoAdapter", "4.8.0.2.0"
  pod "TPNMediationVungleAdapter", "7.7.1.2.0"
  pod "TPNMediationUnityAdsAdapter", "4.17.0.2.0"
  pod "TPNMediationIronSourceAdapter", "9.3.0.0.2.0"
  pod "TPNMediationBigoAdapter", "5.2.0.2.0"
  pod "TPNMediationPubNativeAdapter", "3.8.1.2.0"
  pod "TPNMediationPangleAdapter", "8.1.0.6.2.0"
  pod "TPNMediationFacebookAdapter", "6.21.1.2.0"
  pod "TPNMediationAdmobAdapter", "13.4.0.2.0"
  pod "TPNMediationInmobiAdapter", "11.1.1.2.0"
  pod "TPNMediationApplovinAdapter", "13.6.0.2.1"
  pod "TPNMediationMintegralAdapter", "8.1.3.2.1"
  pod "TPNMediationBidMachineAdapter", "3.7.1.2.1"
  pod "TPNMediationChartboostAdapter", "9.11.0.2.0"
  pod "TPNMediationYandexAdapter", "8.2.0.2.0"
  pod "TPNMediationFyberAdapter", "8.4.7.2.0"

  pod "AppLovinMediationMolocoAdapter", "4.8.0.0"
  pod "AppLovinMediationVungleAdapter", "7.7.1.0"
  pod "AppLovinMediationUnityAdsAdapter", "4.17.0.0"
  pod "AppLovinMediationIronSourceAdapter", "9.3.0.0.0"
  pod "AppLovinMediationBigoAdsAdapter", "5.2.0.0"
  pod "AppLovinMediationVerveAdapter", "3.8.1.0"
  pod "AppLovinMediationLineAdapter", "3.0.1.0"
  pod "AppLovinMediationByteDanceAdapter", "8.1.0.6.0"
  pod "AppLovinMediationFacebookAdapter", "6.21.1.0"
  pod "AppLovinMediationGoogleAdManagerAdapter", "13.4.0.0"
  pod "AppLovinMediationGoogleAdapter", "13.4.0.0"
  pod "AppLovinMediationInMobiAdapter", "11.1.1.0"
  pod "AppLovinMediationMintegralAdapter", "8.1.3.0.0"
  pod "AppLovinMediationBidMachineAdapter", "3.7.1.0.0"
  pod "AppLovinMediationChartboostAdapter", "9.11.0.0"
  pod "AppLovinMediationMyTargetAdapter", "5.43.0.0"
  pod "AppLovinMediationYandexAdapter", "8.2.0.0"
  pod "AppLovinMediationFyberAdapter", "8.4.7.0"

  pod "GoogleMobileAdsMediationVungle", "7.7.1.0"
  pod "GoogleMobileAdsMediationUnity", "4.17.0.0"
  pod "GoogleMobileAdsMediationIronSource", "9.3.0.0.1"
  pod "GoogleMobileAdsMediationLine", "3.0.1.2"
  pod "GoogleMobileAdsMediationPangle", "8.1.0.6.0"
  pod "GoogleMobileAdsMediationFacebook", "6.21.1.1.1"
  pod "GoogleMobileAdsMediationInMobi", "11.1.1.1"
  pod "GoogleMobileAdsMediationAppLovin", "13.6.0.0"
  pod "GoogleMobileAdsMediationMintegral", "8.1.3.0"
  pod "GoogleMobileAdsMediationChartboost", "9.11.0.3"
  pod "GoogleMobileAdsMediationMyTarget", "5.43.0.0.1"
  pod "GoogleMobileAdsMediationFyber", "8.4.7.1.1"


end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    # Pod resource-bundle/framework targets carry no signing team; the app's
    # embed phase re-signs everything with the app identity, so skip it here.
    target.build_configurations.each do |config|
      config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
      config.build_settings['CODE_SIGNING_REQUIRED'] = 'NO'
      config.build_settings['CODE_SIGN_IDENTITY'] = ''
      # Xcode 27 rejects deployment targets below iOS 15; lift every pod to it.
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
    end
  end
end
